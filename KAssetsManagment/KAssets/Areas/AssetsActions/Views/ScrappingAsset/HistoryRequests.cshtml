@model List<KAssets.Areas.AssetsActions.Models.RequestForScrappingViewModel>
@{
    ViewBag.Title = "History";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ScrappingTr.HistoryRequestsForScrapping</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().FromName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().AssetInvNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().DateOfSend)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FromName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssetInvNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfSend)
            </td>
            <td>
                @Html.ActionLink(Common.View, "ViewHistoryRequest", new { id = item.Id }, new { @class = "btn btn-default" })
            </td>
        </tr>
    }

</table>

@if(Model.Count==0)
{
    <h4>@ScrappingTr.NoHistory</h4>
}